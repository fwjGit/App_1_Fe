const host = '127.0.0.1'
const port = '7002'
const protocol = 'http'
const wsPort = '7001'

const url = `${protocol}://${host}:${port}`
const prefix = '/api'

export { url, host, prefix, port, wsPort }
